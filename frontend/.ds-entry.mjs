// Entry stub for the Claude Design tokens-only sync.
//
// Xylona is a Vue 3 + Quasar application, not a React component library, so
// there are no components to expose on window.Xylona. This empty module is
// what the converter bundles into an empty-bodied _ds_bundle.js; the real
// payload of this sync is the token/font/guideline layer reachable from
// styles.css. See .design-sync/NOTES.md.
export {};
