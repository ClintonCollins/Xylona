// Code generated by BobGen sqlite v0.25.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"time"

	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/clause"
	"github.com/stephenafamo/bob/dialect/sqlite"
	"github.com/stephenafamo/bob/dialect/sqlite/dialect"
	"github.com/stephenafamo/bob/dialect/sqlite/im"
	"github.com/stephenafamo/bob/dialect/sqlite/sm"
	"github.com/stephenafamo/bob/dialect/sqlite/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
)

// User is an object representing the database table.
type User struct {
	ID           string    `db:"id,pk" `
	UserName     string    `db:"user_name" `
	Email        string    `db:"email" `
	FirstName    string    `db:"first_name" `
	LastName     string    `db:"last_name" `
	PasswordHash string    `db:"password_hash" `
	SuperUser    bool      `db:"super_user" `
	LastLoginAt  time.Time `db:"last_login_at" `
	CreatedAt    time.Time `db:"created_at" `
	UpdatedAt    time.Time `db:"updated_at" `

	R userR `db:"-" `
}

// UserSlice is an alias for a slice of pointers to User.
// This should almost always be used instead of []*User.
type UserSlice []*User

// Users contains methods to work with the user table
var Users = sqlite.NewTablex[*User, UserSlice, *UserSetter]("", "user")

// UsersQuery is a query on the user table
type UsersQuery = *sqlite.ViewQuery[*User, UserSlice]

// UsersStmt is a prepared statment on user
type UsersStmt = bob.QueryStmt[*User, UserSlice]

// userR is where relationships are stored.
type userR struct {
	GameServers  GameServerSlice  // fk_game_server_2
	Logs         LogSlice         // fk_log_2
	UserAPIKeys  UserAPIKeySlice  // fk_user_api_key_0
	UserSessions UserSessionSlice // fk_user_session_0
}

// UserSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type UserSetter struct {
	ID           omit.Val[string]    `db:"id,pk"`
	UserName     omit.Val[string]    `db:"user_name"`
	Email        omit.Val[string]    `db:"email"`
	FirstName    omit.Val[string]    `db:"first_name"`
	LastName     omit.Val[string]    `db:"last_name"`
	PasswordHash omit.Val[string]    `db:"password_hash"`
	SuperUser    omit.Val[bool]      `db:"super_user"`
	LastLoginAt  omit.Val[time.Time] `db:"last_login_at"`
	CreatedAt    omit.Val[time.Time] `db:"created_at"`
	UpdatedAt    omit.Val[time.Time] `db:"updated_at"`
}

func (s UserSetter) SetColumns() []string {
	vals := make([]string, 0, 10)
	if !s.ID.IsUnset() {
		vals = append(vals, "id")
	}

	if !s.UserName.IsUnset() {
		vals = append(vals, "user_name")
	}

	if !s.Email.IsUnset() {
		vals = append(vals, "email")
	}

	if !s.FirstName.IsUnset() {
		vals = append(vals, "first_name")
	}

	if !s.LastName.IsUnset() {
		vals = append(vals, "last_name")
	}

	if !s.PasswordHash.IsUnset() {
		vals = append(vals, "password_hash")
	}

	if !s.SuperUser.IsUnset() {
		vals = append(vals, "super_user")
	}

	if !s.LastLoginAt.IsUnset() {
		vals = append(vals, "last_login_at")
	}

	if !s.CreatedAt.IsUnset() {
		vals = append(vals, "created_at")
	}

	if !s.UpdatedAt.IsUnset() {
		vals = append(vals, "updated_at")
	}

	return vals
}

func (s UserSetter) Overwrite(t *User) {
	if !s.ID.IsUnset() {
		t.ID, _ = s.ID.Get()
	}
	if !s.UserName.IsUnset() {
		t.UserName, _ = s.UserName.Get()
	}
	if !s.Email.IsUnset() {
		t.Email, _ = s.Email.Get()
	}
	if !s.FirstName.IsUnset() {
		t.FirstName, _ = s.FirstName.Get()
	}
	if !s.LastName.IsUnset() {
		t.LastName, _ = s.LastName.Get()
	}
	if !s.PasswordHash.IsUnset() {
		t.PasswordHash, _ = s.PasswordHash.Get()
	}
	if !s.SuperUser.IsUnset() {
		t.SuperUser, _ = s.SuperUser.Get()
	}
	if !s.LastLoginAt.IsUnset() {
		t.LastLoginAt, _ = s.LastLoginAt.Get()
	}
	if !s.CreatedAt.IsUnset() {
		t.CreatedAt, _ = s.CreatedAt.Get()
	}
	if !s.UpdatedAt.IsUnset() {
		t.UpdatedAt, _ = s.UpdatedAt.Get()
	}
}

func (s UserSetter) InsertMod() bob.Mod[*dialect.InsertQuery] {
	vals := make([]bob.Expression, 0, 10)
	if !s.ID.IsUnset() {
		vals = append(vals, sqlite.Arg(s.ID))
	}

	if !s.UserName.IsUnset() {
		vals = append(vals, sqlite.Arg(s.UserName))
	}

	if !s.Email.IsUnset() {
		vals = append(vals, sqlite.Arg(s.Email))
	}

	if !s.FirstName.IsUnset() {
		vals = append(vals, sqlite.Arg(s.FirstName))
	}

	if !s.LastName.IsUnset() {
		vals = append(vals, sqlite.Arg(s.LastName))
	}

	if !s.PasswordHash.IsUnset() {
		vals = append(vals, sqlite.Arg(s.PasswordHash))
	}

	if !s.SuperUser.IsUnset() {
		vals = append(vals, sqlite.Arg(s.SuperUser))
	}

	if !s.LastLoginAt.IsUnset() {
		vals = append(vals, sqlite.Arg(s.LastLoginAt))
	}

	if !s.CreatedAt.IsUnset() {
		vals = append(vals, sqlite.Arg(s.CreatedAt))
	}

	if !s.UpdatedAt.IsUnset() {
		vals = append(vals, sqlite.Arg(s.UpdatedAt))
	}

	return im.Values(vals...)
}

func (s UserSetter) Apply(q *dialect.UpdateQuery) {
	um.Set(s.Expressions()...).Apply(q)
}

func (s UserSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 10)

	if !s.ID.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "id")...),
			sqlite.Arg(s.ID),
		}})
	}

	if !s.UserName.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "user_name")...),
			sqlite.Arg(s.UserName),
		}})
	}

	if !s.Email.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "email")...),
			sqlite.Arg(s.Email),
		}})
	}

	if !s.FirstName.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "first_name")...),
			sqlite.Arg(s.FirstName),
		}})
	}

	if !s.LastName.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "last_name")...),
			sqlite.Arg(s.LastName),
		}})
	}

	if !s.PasswordHash.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "password_hash")...),
			sqlite.Arg(s.PasswordHash),
		}})
	}

	if !s.SuperUser.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "super_user")...),
			sqlite.Arg(s.SuperUser),
		}})
	}

	if !s.LastLoginAt.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "last_login_at")...),
			sqlite.Arg(s.LastLoginAt),
		}})
	}

	if !s.CreatedAt.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "created_at")...),
			sqlite.Arg(s.CreatedAt),
		}})
	}

	if !s.UpdatedAt.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "updated_at")...),
			sqlite.Arg(s.UpdatedAt),
		}})
	}

	return exprs
}

type userColumnNames struct {
	ID           string
	UserName     string
	Email        string
	FirstName    string
	LastName     string
	PasswordHash string
	SuperUser    string
	LastLoginAt  string
	CreatedAt    string
	UpdatedAt    string
}

type userRelationshipJoins[Q dialect.Joinable] struct {
	GameServers  bob.Mod[Q]
	Logs         bob.Mod[Q]
	UserAPIKeys  bob.Mod[Q]
	UserSessions bob.Mod[Q]
}

func buildUserRelationshipJoins[Q dialect.Joinable](ctx context.Context, typ string) userRelationshipJoins[Q] {
	return userRelationshipJoins[Q]{
		GameServers:  usersJoinGameServers[Q](ctx, typ),
		Logs:         usersJoinLogs[Q](ctx, typ),
		UserAPIKeys:  usersJoinUserAPIKeys[Q](ctx, typ),
		UserSessions: usersJoinUserSessions[Q](ctx, typ),
	}
}

func usersJoin[Q dialect.Joinable](ctx context.Context) joinSet[userRelationshipJoins[Q]] {
	return joinSet[userRelationshipJoins[Q]]{
		InnerJoin: buildUserRelationshipJoins[Q](ctx, clause.InnerJoin),
		LeftJoin:  buildUserRelationshipJoins[Q](ctx, clause.LeftJoin),
		RightJoin: buildUserRelationshipJoins[Q](ctx, clause.RightJoin),
	}
}

var UserColumns = struct {
	ID           sqlite.Expression
	UserName     sqlite.Expression
	Email        sqlite.Expression
	FirstName    sqlite.Expression
	LastName     sqlite.Expression
	PasswordHash sqlite.Expression
	SuperUser    sqlite.Expression
	LastLoginAt  sqlite.Expression
	CreatedAt    sqlite.Expression
	UpdatedAt    sqlite.Expression
}{
	ID:           sqlite.Quote("user", "id"),
	UserName:     sqlite.Quote("user", "user_name"),
	Email:        sqlite.Quote("user", "email"),
	FirstName:    sqlite.Quote("user", "first_name"),
	LastName:     sqlite.Quote("user", "last_name"),
	PasswordHash: sqlite.Quote("user", "password_hash"),
	SuperUser:    sqlite.Quote("user", "super_user"),
	LastLoginAt:  sqlite.Quote("user", "last_login_at"),
	CreatedAt:    sqlite.Quote("user", "created_at"),
	UpdatedAt:    sqlite.Quote("user", "updated_at"),
}

type userWhere[Q sqlite.Filterable] struct {
	ID           sqlite.WhereMod[Q, string]
	UserName     sqlite.WhereMod[Q, string]
	Email        sqlite.WhereMod[Q, string]
	FirstName    sqlite.WhereMod[Q, string]
	LastName     sqlite.WhereMod[Q, string]
	PasswordHash sqlite.WhereMod[Q, string]
	SuperUser    sqlite.WhereMod[Q, bool]
	LastLoginAt  sqlite.WhereMod[Q, time.Time]
	CreatedAt    sqlite.WhereMod[Q, time.Time]
	UpdatedAt    sqlite.WhereMod[Q, time.Time]
}

func UserWhere[Q sqlite.Filterable]() userWhere[Q] {
	return userWhere[Q]{
		ID:           sqlite.Where[Q, string](UserColumns.ID),
		UserName:     sqlite.Where[Q, string](UserColumns.UserName),
		Email:        sqlite.Where[Q, string](UserColumns.Email),
		FirstName:    sqlite.Where[Q, string](UserColumns.FirstName),
		LastName:     sqlite.Where[Q, string](UserColumns.LastName),
		PasswordHash: sqlite.Where[Q, string](UserColumns.PasswordHash),
		SuperUser:    sqlite.Where[Q, bool](UserColumns.SuperUser),
		LastLoginAt:  sqlite.Where[Q, time.Time](UserColumns.LastLoginAt),
		CreatedAt:    sqlite.Where[Q, time.Time](UserColumns.CreatedAt),
		UpdatedAt:    sqlite.Where[Q, time.Time](UserColumns.UpdatedAt),
	}
}

// FindUser retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindUser(ctx context.Context, exec bob.Executor, IDPK string, cols ...string) (*User, error) {
	if len(cols) == 0 {
		return Users.Query(
			ctx, exec,
			SelectWhere.Users.ID.EQ(IDPK),
		).One()
	}

	return Users.Query(
		ctx, exec,
		SelectWhere.Users.ID.EQ(IDPK),
		sm.Columns(Users.Columns().Only(cols...)),
	).One()
}

// UserExists checks the presence of a single record by primary key
func UserExists(ctx context.Context, exec bob.Executor, IDPK string) (bool, error) {
	return Users.Query(
		ctx, exec,
		SelectWhere.Users.ID.EQ(IDPK),
	).Exists()
}

// PrimaryKeyVals returns the primary key values of the User
func (o *User) PrimaryKeyVals() bob.Expression {
	return sqlite.Arg(o.ID)
}

// Update uses an executor to update the User
func (o *User) Update(ctx context.Context, exec bob.Executor, s *UserSetter) error {
	return Users.Update(ctx, exec, s, o)
}

// Delete deletes a single User record with an executor
func (o *User) Delete(ctx context.Context, exec bob.Executor) error {
	return Users.Delete(ctx, exec, o)
}

// Reload refreshes the User using the executor
func (o *User) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := Users.Query(
		ctx, exec,
		SelectWhere.Users.ID.EQ(o.ID),
	).One()
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

func (o UserSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals UserSetter) error {
	return Users.Update(ctx, exec, &vals, o...)
}

func (o UserSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	return Users.Delete(ctx, exec, o...)
}

func (o UserSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	var mods []bob.Mod[*dialect.SelectQuery]

	IDPK := make([]string, len(o))

	for i, o := range o {
		IDPK[i] = o.ID
	}

	mods = append(mods,
		SelectWhere.Users.ID.In(IDPK...),
	)

	o2, err := Users.Query(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, old := range o {
		for _, new := range o2 {
			if new.ID != old.ID {
				continue
			}
			new.R = old.R
			*old = *new
			break
		}
	}

	return nil
}

func usersJoinGameServers[Q dialect.Joinable](ctx context.Context, typ string) bob.Mod[Q] {
	return mods.QueryMods[Q]{
		dialect.Join[Q](typ, GameServers.NameAs(ctx)).On(
			GameServerColumns.UserID.EQ(UserColumns.ID),
		),
	}
}

func usersJoinLogs[Q dialect.Joinable](ctx context.Context, typ string) bob.Mod[Q] {
	return mods.QueryMods[Q]{
		dialect.Join[Q](typ, Logs.NameAs(ctx)).On(
			LogColumns.UserID.EQ(UserColumns.ID),
		),
	}
}

func usersJoinUserAPIKeys[Q dialect.Joinable](ctx context.Context, typ string) bob.Mod[Q] {
	return mods.QueryMods[Q]{
		dialect.Join[Q](typ, UserAPIKeys.NameAs(ctx)).On(
			UserAPIKeyColumns.UserID.EQ(UserColumns.ID),
		),
	}
}

func usersJoinUserSessions[Q dialect.Joinable](ctx context.Context, typ string) bob.Mod[Q] {
	return mods.QueryMods[Q]{
		dialect.Join[Q](typ, UserSessions.NameAs(ctx)).On(
			UserSessionColumns.UserID.EQ(UserColumns.ID),
		),
	}
}

// GameServers starts a query for related objects on game_server
func (o *User) GameServers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) GameServersQuery {
	return GameServers.Query(ctx, exec, append(mods,
		sm.Where(GameServerColumns.UserID.EQ(sqlite.Arg(o.ID))),
	)...)
}

func (os UserSlice) GameServers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) GameServersQuery {
	PKArgs := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgs[i] = sqlite.ArgGroup(o.ID)
	}

	return GameServers.Query(ctx, exec, append(mods,
		sm.Where(sqlite.Group(GameServerColumns.UserID).In(PKArgs...)),
	)...)
}

// Logs starts a query for related objects on log
func (o *User) Logs(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) LogsQuery {
	return Logs.Query(ctx, exec, append(mods,
		sm.Where(LogColumns.UserID.EQ(sqlite.Arg(o.ID))),
	)...)
}

func (os UserSlice) Logs(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) LogsQuery {
	PKArgs := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgs[i] = sqlite.ArgGroup(o.ID)
	}

	return Logs.Query(ctx, exec, append(mods,
		sm.Where(sqlite.Group(LogColumns.UserID).In(PKArgs...)),
	)...)
}

// UserAPIKeys starts a query for related objects on user_api_key
func (o *User) UserAPIKeys(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) UserAPIKeysQuery {
	return UserAPIKeys.Query(ctx, exec, append(mods,
		sm.Where(UserAPIKeyColumns.UserID.EQ(sqlite.Arg(o.ID))),
	)...)
}

func (os UserSlice) UserAPIKeys(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) UserAPIKeysQuery {
	PKArgs := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgs[i] = sqlite.ArgGroup(o.ID)
	}

	return UserAPIKeys.Query(ctx, exec, append(mods,
		sm.Where(sqlite.Group(UserAPIKeyColumns.UserID).In(PKArgs...)),
	)...)
}

// UserSessions starts a query for related objects on user_session
func (o *User) UserSessions(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) UserSessionsQuery {
	return UserSessions.Query(ctx, exec, append(mods,
		sm.Where(UserSessionColumns.UserID.EQ(sqlite.Arg(o.ID))),
	)...)
}

func (os UserSlice) UserSessions(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) UserSessionsQuery {
	PKArgs := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgs[i] = sqlite.ArgGroup(o.ID)
	}

	return UserSessions.Query(ctx, exec, append(mods,
		sm.Where(sqlite.Group(UserSessionColumns.UserID).In(PKArgs...)),
	)...)
}

func (o *User) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "GameServers":
		rels, ok := retrieved.(GameServerSlice)
		if !ok {
			return fmt.Errorf("user cannot load %T as %q", retrieved, name)
		}

		o.R.GameServers = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.User = o
			}
		}
		return nil
	case "Logs":
		rels, ok := retrieved.(LogSlice)
		if !ok {
			return fmt.Errorf("user cannot load %T as %q", retrieved, name)
		}

		o.R.Logs = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.User = o
			}
		}
		return nil
	case "UserAPIKeys":
		rels, ok := retrieved.(UserAPIKeySlice)
		if !ok {
			return fmt.Errorf("user cannot load %T as %q", retrieved, name)
		}

		o.R.UserAPIKeys = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.User = o
			}
		}
		return nil
	case "UserSessions":
		rels, ok := retrieved.(UserSessionSlice)
		if !ok {
			return fmt.Errorf("user cannot load %T as %q", retrieved, name)
		}

		o.R.UserSessions = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.User = o
			}
		}
		return nil
	default:
		return fmt.Errorf("user has no relationship %q", name)
	}
}

func ThenLoadUserGameServers(queryMods ...bob.Mod[*dialect.SelectQuery]) sqlite.Loader {
	return sqlite.Loader(func(ctx context.Context, exec bob.Executor, retrieved any) error {
		loader, isLoader := retrieved.(interface {
			LoadUserGameServers(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
		})
		if !isLoader {
			return fmt.Errorf("object %T cannot load UserGameServers", retrieved)
		}

		err := loader.LoadUserGameServers(ctx, exec, queryMods...)

		// Don't cause an issue due to missing relationships
		if errors.Is(err, sql.ErrNoRows) {
			return nil
		}

		return err
	})
}

// LoadUserGameServers loads the user's GameServers into the .R struct
func (o *User) LoadUserGameServers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.GameServers = nil

	related, err := o.GameServers(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.User = o
	}

	o.R.GameServers = related
	return nil
}

// LoadUserGameServers loads the user's GameServers into the .R struct
func (os UserSlice) LoadUserGameServers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	gameServers, err := os.GameServers(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, o := range os {
		o.R.GameServers = nil
	}

	for _, o := range os {
		for _, rel := range gameServers {
			if o.ID != rel.UserID {
				continue
			}

			rel.R.User = o

			o.R.GameServers = append(o.R.GameServers, rel)
		}
	}

	return nil
}

func ThenLoadUserLogs(queryMods ...bob.Mod[*dialect.SelectQuery]) sqlite.Loader {
	return sqlite.Loader(func(ctx context.Context, exec bob.Executor, retrieved any) error {
		loader, isLoader := retrieved.(interface {
			LoadUserLogs(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
		})
		if !isLoader {
			return fmt.Errorf("object %T cannot load UserLogs", retrieved)
		}

		err := loader.LoadUserLogs(ctx, exec, queryMods...)

		// Don't cause an issue due to missing relationships
		if errors.Is(err, sql.ErrNoRows) {
			return nil
		}

		return err
	})
}

// LoadUserLogs loads the user's Logs into the .R struct
func (o *User) LoadUserLogs(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.Logs = nil

	related, err := o.Logs(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.User = o
	}

	o.R.Logs = related
	return nil
}

// LoadUserLogs loads the user's Logs into the .R struct
func (os UserSlice) LoadUserLogs(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	logs, err := os.Logs(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, o := range os {
		o.R.Logs = nil
	}

	for _, o := range os {
		for _, rel := range logs {
			if o.ID != rel.UserID.GetOrZero() {
				continue
			}

			rel.R.User = o

			o.R.Logs = append(o.R.Logs, rel)
		}
	}

	return nil
}

func ThenLoadUserUserAPIKeys(queryMods ...bob.Mod[*dialect.SelectQuery]) sqlite.Loader {
	return sqlite.Loader(func(ctx context.Context, exec bob.Executor, retrieved any) error {
		loader, isLoader := retrieved.(interface {
			LoadUserUserAPIKeys(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
		})
		if !isLoader {
			return fmt.Errorf("object %T cannot load UserUserAPIKeys", retrieved)
		}

		err := loader.LoadUserUserAPIKeys(ctx, exec, queryMods...)

		// Don't cause an issue due to missing relationships
		if errors.Is(err, sql.ErrNoRows) {
			return nil
		}

		return err
	})
}

// LoadUserUserAPIKeys loads the user's UserAPIKeys into the .R struct
func (o *User) LoadUserUserAPIKeys(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.UserAPIKeys = nil

	related, err := o.UserAPIKeys(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.User = o
	}

	o.R.UserAPIKeys = related
	return nil
}

// LoadUserUserAPIKeys loads the user's UserAPIKeys into the .R struct
func (os UserSlice) LoadUserUserAPIKeys(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	userAPIKeys, err := os.UserAPIKeys(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, o := range os {
		o.R.UserAPIKeys = nil
	}

	for _, o := range os {
		for _, rel := range userAPIKeys {
			if o.ID != rel.UserID.GetOrZero() {
				continue
			}

			rel.R.User = o

			o.R.UserAPIKeys = append(o.R.UserAPIKeys, rel)
		}
	}

	return nil
}

func ThenLoadUserUserSessions(queryMods ...bob.Mod[*dialect.SelectQuery]) sqlite.Loader {
	return sqlite.Loader(func(ctx context.Context, exec bob.Executor, retrieved any) error {
		loader, isLoader := retrieved.(interface {
			LoadUserUserSessions(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
		})
		if !isLoader {
			return fmt.Errorf("object %T cannot load UserUserSessions", retrieved)
		}

		err := loader.LoadUserUserSessions(ctx, exec, queryMods...)

		// Don't cause an issue due to missing relationships
		if errors.Is(err, sql.ErrNoRows) {
			return nil
		}

		return err
	})
}

// LoadUserUserSessions loads the user's UserSessions into the .R struct
func (o *User) LoadUserUserSessions(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.UserSessions = nil

	related, err := o.UserSessions(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.User = o
	}

	o.R.UserSessions = related
	return nil
}

// LoadUserUserSessions loads the user's UserSessions into the .R struct
func (os UserSlice) LoadUserUserSessions(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	userSessions, err := os.UserSessions(ctx, exec, mods...).All()
	if err != nil {
		return err
	}

	for _, o := range os {
		o.R.UserSessions = nil
	}

	for _, o := range os {
		for _, rel := range userSessions {
			if o.ID != rel.UserID {
				continue
			}

			rel.R.User = o

			o.R.UserSessions = append(o.R.UserSessions, rel)
		}
	}

	return nil
}

func insertUserGameServers0(ctx context.Context, exec bob.Executor, gameServers1 []*GameServerSetter, user0 *User) (GameServerSlice, error) {
	for i := range gameServers1 {
		gameServers1[i].UserID = omit.From(user0.ID)
	}

	ret, err := GameServers.InsertMany(ctx, exec, gameServers1...)
	if err != nil {
		return ret, fmt.Errorf("insertUserGameServers0: %w", err)
	}

	return ret, nil
}

func attachUserGameServers0(ctx context.Context, exec bob.Executor, count int, gameServers1 GameServerSlice, user0 *User) (GameServerSlice, error) {
	setter := &GameServerSetter{
		UserID: omit.From(user0.ID),
	}

	err := GameServers.Update(ctx, exec, setter, gameServers1...)
	if err != nil {
		return nil, fmt.Errorf("attachUserGameServers0: %w", err)
	}

	return gameServers1, nil
}

func (user0 *User) InsertGameServers(ctx context.Context, exec bob.Executor, related ...*GameServerSetter) error {
	if len(related) == 0 {
		return nil
	}

	gameServers1, err := insertUserGameServers0(ctx, exec, related, user0)
	if err != nil {
		return err
	}

	user0.R.GameServers = append(user0.R.GameServers, gameServers1...)

	for _, rel := range gameServers1 {
		rel.R.User = user0
	}
	return nil
}

func (user0 *User) AttachGameServers(ctx context.Context, exec bob.Executor, related ...*GameServer) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	gameServers1 := GameServerSlice(related)

	_, err = attachUserGameServers0(ctx, exec, len(related), gameServers1, user0)
	if err != nil {
		return err
	}

	user0.R.GameServers = append(user0.R.GameServers, gameServers1...)

	for _, rel := range related {
		rel.R.User = user0
	}

	return nil
}

func insertUserLogs0(ctx context.Context, exec bob.Executor, logs1 []*LogSetter, user0 *User) (LogSlice, error) {
	for i := range logs1 {
		logs1[i].UserID = omitnull.From(user0.ID)
	}

	ret, err := Logs.InsertMany(ctx, exec, logs1...)
	if err != nil {
		return ret, fmt.Errorf("insertUserLogs0: %w", err)
	}

	return ret, nil
}

func attachUserLogs0(ctx context.Context, exec bob.Executor, count int, logs1 LogSlice, user0 *User) (LogSlice, error) {
	setter := &LogSetter{
		UserID: omitnull.From(user0.ID),
	}

	err := Logs.Update(ctx, exec, setter, logs1...)
	if err != nil {
		return nil, fmt.Errorf("attachUserLogs0: %w", err)
	}

	return logs1, nil
}

func (user0 *User) InsertLogs(ctx context.Context, exec bob.Executor, related ...*LogSetter) error {
	if len(related) == 0 {
		return nil
	}

	logs1, err := insertUserLogs0(ctx, exec, related, user0)
	if err != nil {
		return err
	}

	user0.R.Logs = append(user0.R.Logs, logs1...)

	for _, rel := range logs1 {
		rel.R.User = user0
	}
	return nil
}

func (user0 *User) AttachLogs(ctx context.Context, exec bob.Executor, related ...*Log) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	logs1 := LogSlice(related)

	_, err = attachUserLogs0(ctx, exec, len(related), logs1, user0)
	if err != nil {
		return err
	}

	user0.R.Logs = append(user0.R.Logs, logs1...)

	for _, rel := range related {
		rel.R.User = user0
	}

	return nil
}

func insertUserUserAPIKeys0(ctx context.Context, exec bob.Executor, userAPIKeys1 []*UserAPIKeySetter, user0 *User) (UserAPIKeySlice, error) {
	for i := range userAPIKeys1 {
		userAPIKeys1[i].UserID = omitnull.From(user0.ID)
	}

	ret, err := UserAPIKeys.InsertMany(ctx, exec, userAPIKeys1...)
	if err != nil {
		return ret, fmt.Errorf("insertUserUserAPIKeys0: %w", err)
	}

	return ret, nil
}

func attachUserUserAPIKeys0(ctx context.Context, exec bob.Executor, count int, userAPIKeys1 UserAPIKeySlice, user0 *User) (UserAPIKeySlice, error) {
	setter := &UserAPIKeySetter{
		UserID: omitnull.From(user0.ID),
	}

	err := UserAPIKeys.Update(ctx, exec, setter, userAPIKeys1...)
	if err != nil {
		return nil, fmt.Errorf("attachUserUserAPIKeys0: %w", err)
	}

	return userAPIKeys1, nil
}

func (user0 *User) InsertUserAPIKeys(ctx context.Context, exec bob.Executor, related ...*UserAPIKeySetter) error {
	if len(related) == 0 {
		return nil
	}

	userAPIKeys1, err := insertUserUserAPIKeys0(ctx, exec, related, user0)
	if err != nil {
		return err
	}

	user0.R.UserAPIKeys = append(user0.R.UserAPIKeys, userAPIKeys1...)

	for _, rel := range userAPIKeys1 {
		rel.R.User = user0
	}
	return nil
}

func (user0 *User) AttachUserAPIKeys(ctx context.Context, exec bob.Executor, related ...*UserAPIKey) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	userAPIKeys1 := UserAPIKeySlice(related)

	_, err = attachUserUserAPIKeys0(ctx, exec, len(related), userAPIKeys1, user0)
	if err != nil {
		return err
	}

	user0.R.UserAPIKeys = append(user0.R.UserAPIKeys, userAPIKeys1...)

	for _, rel := range related {
		rel.R.User = user0
	}

	return nil
}

func insertUserUserSessions0(ctx context.Context, exec bob.Executor, userSessions1 []*UserSessionSetter, user0 *User) (UserSessionSlice, error) {
	for i := range userSessions1 {
		userSessions1[i].UserID = omit.From(user0.ID)
	}

	ret, err := UserSessions.InsertMany(ctx, exec, userSessions1...)
	if err != nil {
		return ret, fmt.Errorf("insertUserUserSessions0: %w", err)
	}

	return ret, nil
}

func attachUserUserSessions0(ctx context.Context, exec bob.Executor, count int, userSessions1 UserSessionSlice, user0 *User) (UserSessionSlice, error) {
	setter := &UserSessionSetter{
		UserID: omit.From(user0.ID),
	}

	err := UserSessions.Update(ctx, exec, setter, userSessions1...)
	if err != nil {
		return nil, fmt.Errorf("attachUserUserSessions0: %w", err)
	}

	return userSessions1, nil
}

func (user0 *User) InsertUserSessions(ctx context.Context, exec bob.Executor, related ...*UserSessionSetter) error {
	if len(related) == 0 {
		return nil
	}

	userSessions1, err := insertUserUserSessions0(ctx, exec, related, user0)
	if err != nil {
		return err
	}

	user0.R.UserSessions = append(user0.R.UserSessions, userSessions1...)

	for _, rel := range userSessions1 {
		rel.R.User = user0
	}
	return nil
}

func (user0 *User) AttachUserSessions(ctx context.Context, exec bob.Executor, related ...*UserSession) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	userSessions1 := UserSessionSlice(related)

	_, err = attachUserUserSessions0(ctx, exec, len(related), userSessions1, user0)
	if err != nil {
		return err
	}

	user0.R.UserSessions = append(user0.R.UserSessions, userSessions1...)

	for _, rel := range related {
		rel.R.User = user0
	}

	return nil
}
