// Code generated by BobGen sqlite v0.48.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"io"
	"time"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/sqlite"
	"github.com/stephenafamo/bob/dialect/sqlite/dialect"
	"github.com/stephenafamo/bob/dialect/sqlite/dm"
	"github.com/stephenafamo/bob/dialect/sqlite/sm"
	"github.com/stephenafamo/bob/dialect/sqlite/um"
	"github.com/stephenafamo/bob/expr"
)

// RevokedJWT is an object representing the database table.
type RevokedJWT struct {
	ID                    string              `db:"id,pk" `
	JWTID                 null.Val[string]    `db:"jwt_id" `
	Username              null.Val[string]    `db:"username" `
	DeleteAllTokensBefore null.Val[time.Time] `db:"delete_all_tokens_before" `
	CreatedAt             time.Time           `db:"created_at" `
}

// RevokedJWTSlice is an alias for a slice of pointers to RevokedJWT.
// This should almost always be used instead of []*RevokedJWT.
type RevokedJWTSlice []*RevokedJWT

// RevokedJWTS contains methods to work with the revoked_jwt table
var RevokedJWTS = sqlite.NewTablex[*RevokedJWT, RevokedJWTSlice, *RevokedJWTSetter]("", "revoked_jwt", buildRevokedJWTColumns("revoked_jwt"))

// RevokedJWTSQuery is a query on the revoked_jwt table
type RevokedJWTSQuery = *sqlite.ViewQuery[*RevokedJWT, RevokedJWTSlice]

func buildRevokedJWTColumns(tableName string) revokedJWTColumns {
	columnsExpr := expr.NewColumnsExpr(
		"id", "jwt_id", "username", "delete_all_tokens_before", "created_at",
	)

	if tableName != "" {
		columnsExpr = columnsExpr.WithParent(tableName)
	}

	return revokedJWTColumns{
		ColumnsExpr:           columnsExpr,
		tableAlias:            tableName,
		ID:                    buildRevokedJWTColumn(tableName, "id"),
		JWTID:                 buildRevokedJWTColumn(tableName, "jwt_id"),
		Username:              buildRevokedJWTColumn(tableName, "username"),
		DeleteAllTokensBefore: buildRevokedJWTColumn(tableName, "delete_all_tokens_before"),
		CreatedAt:             buildRevokedJWTColumn(tableName, "created_at"),
	}
}

type revokedJWTColumns struct {
	expr.ColumnsExpr
	tableAlias            string
	ID                    revokedJWTColumn
	JWTID                 revokedJWTColumn
	Username              revokedJWTColumn
	DeleteAllTokensBefore revokedJWTColumn
	CreatedAt             revokedJWTColumn
}

// Alias returns the current table alias for the columns set.
func (c revokedJWTColumns) Alias() string {
	return c.tableAlias
}

// AliasedAs returns a copy of the columns set qualified by tableName.
func (revokedJWTColumns) AliasedAs(tableName string) revokedJWTColumns {
	return buildRevokedJWTColumns(tableName)
}

// Unqualified returns a copy of the columns set without table qualification.
func (c revokedJWTColumns) Unqualified() revokedJWTColumns {
	return buildRevokedJWTColumns("")
}

func buildRevokedJWTColumn(alias, name string) revokedJWTColumn {
	return revokedJWTColumn{
		Expression: sqlite.Quote(alias, name),
		alias:      alias,
		name:       name,
	}
}

type revokedJWTColumn struct {
	sqlite.Expression
	alias string
	name  string
}

// Name returns the unqualified column name.
func (c revokedJWTColumn) Name() string {
	return c.name
}

// ShouldOmitParens prevents automatic parenthesis wrapping in expression builders.
func (c revokedJWTColumn) ShouldOmitParens() bool {
	return true
}

// RevokedJWTSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type RevokedJWTSetter struct {
	ID                    omit.Val[string]        `db:"id,pk" `
	JWTID                 omitnull.Val[string]    `db:"jwt_id" `
	Username              omitnull.Val[string]    `db:"username" `
	DeleteAllTokensBefore omitnull.Val[time.Time] `db:"delete_all_tokens_before" `
	CreatedAt             omit.Val[time.Time]     `db:"created_at" `
}

func (s RevokedJWTSetter) SetColumns() []string {
	vals := make([]string, 0, 5)
	if s.ID.IsValue() {
		vals = append(vals, "id")
	}
	if s.JWTID.IsValue() || s.JWTID.IsNull() {
		vals = append(vals, "jwt_id")
	}
	if s.Username.IsValue() || s.Username.IsNull() {
		vals = append(vals, "username")
	}
	if s.DeleteAllTokensBefore.IsValue() || s.DeleteAllTokensBefore.IsNull() {
		vals = append(vals, "delete_all_tokens_before")
	}
	if s.CreatedAt.IsValue() {
		vals = append(vals, "created_at")
	}
	return vals
}

func (s RevokedJWTSetter) Overwrite(t *RevokedJWT) {
	if s.ID.IsValue() {
		t.ID = s.ID.MustGet()
	}
	if s.JWTID.IsValue() || s.JWTID.IsNull() {
		t.JWTID = s.JWTID.MustGetNull()
	}
	if s.Username.IsValue() || s.Username.IsNull() {
		t.Username = s.Username.MustGetNull()
	}
	if s.DeleteAllTokensBefore.IsValue() || s.DeleteAllTokensBefore.IsNull() {
		t.DeleteAllTokensBefore = s.DeleteAllTokensBefore.MustGetNull()
	}
	if s.CreatedAt.IsValue() {
		t.CreatedAt = s.CreatedAt.MustGet()
	}
}

func (s *RevokedJWTSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return RevokedJWTS.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	if len(q.TableRef.Columns) == 0 {
		q.TableRef.Columns = s.SetColumns()
		if len(q.TableRef.Columns) == 0 {
			q.TableRef.Columns = []string{"id"}
		}

	}

	vals := make([]bob.Expression, 0, len(q.TableRef.Columns))
	for _, col := range q.TableRef.Columns {
		switch col {
		case "id":
			vals = append(vals, bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
				if s.ID.IsUnset() {
					return sqlite.Arg(nil).WriteSQL(ctx, w, d, start)
				}
				return sqlite.Arg(s.ID.MustGet()).WriteSQL(ctx, w, d, start)
			}))
		case "jwt_id":
			vals = append(vals, bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
				if s.JWTID.IsUnset() {
					return sqlite.Arg(nil).WriteSQL(ctx, w, d, start)
				}
				return sqlite.Arg(s.JWTID.MustGetNull()).WriteSQL(ctx, w, d, start)
			}))
		case "username":
			vals = append(vals, bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
				if s.Username.IsUnset() {
					return sqlite.Arg(nil).WriteSQL(ctx, w, d, start)
				}
				return sqlite.Arg(s.Username.MustGetNull()).WriteSQL(ctx, w, d, start)
			}))
		case "delete_all_tokens_before":
			vals = append(vals, bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
				if s.DeleteAllTokensBefore.IsUnset() {
					return sqlite.Arg(nil).WriteSQL(ctx, w, d, start)
				}
				return sqlite.Arg(s.DeleteAllTokensBefore.MustGetNull()).WriteSQL(ctx, w, d, start)
			}))
		case "created_at":
			vals = append(vals, bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
				if s.CreatedAt.IsUnset() {
					return sqlite.Arg(nil).WriteSQL(ctx, w, d, start)
				}
				return sqlite.Arg(s.CreatedAt.MustGet()).WriteSQL(ctx, w, d, start)
			}))
		}
	}

	q.AppendValues(vals...)
}

func (s RevokedJWTSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s RevokedJWTSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 5)

	if s.ID.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "id")...),
			sqlite.Arg(s.ID),
		}})
	}

	if s.JWTID.IsValue() || s.JWTID.IsNull() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "jwt_id")...),
			sqlite.Arg(s.JWTID),
		}})
	}

	if s.Username.IsValue() || s.Username.IsNull() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "username")...),
			sqlite.Arg(s.Username),
		}})
	}

	if s.DeleteAllTokensBefore.IsValue() || s.DeleteAllTokensBefore.IsNull() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "delete_all_tokens_before")...),
			sqlite.Arg(s.DeleteAllTokensBefore),
		}})
	}

	if s.CreatedAt.IsValue() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "created_at")...),
			sqlite.Arg(s.CreatedAt),
		}})
	}

	return exprs
}

// FindRevokedJWT retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindRevokedJWT(ctx context.Context, exec bob.Executor, IDPK string, cols ...string) (*RevokedJWT, error) {
	if len(cols) == 0 {
		return RevokedJWTS.Query(
			sm.Where(RevokedJWTS.Columns.ID.EQ(sqlite.Arg(IDPK))),
		).One(ctx, exec)
	}

	return RevokedJWTS.Query(
		sm.Where(RevokedJWTS.Columns.ID.EQ(sqlite.Arg(IDPK))),
		sm.Columns(RevokedJWTS.Columns.Only(cols...)),
	).One(ctx, exec)
}

// RevokedJWTExists checks the presence of a single record by primary key
func RevokedJWTExists(ctx context.Context, exec bob.Executor, IDPK string) (bool, error) {
	return RevokedJWTS.Query(
		sm.Where(RevokedJWTS.Columns.ID.EQ(sqlite.Arg(IDPK))),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after RevokedJWT is retrieved from the database
func (o *RevokedJWT) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = RevokedJWTS.AfterSelectHooks.RunHooks(ctx, exec, RevokedJWTSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = RevokedJWTS.AfterInsertHooks.RunHooks(ctx, exec, RevokedJWTSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = RevokedJWTS.AfterUpdateHooks.RunHooks(ctx, exec, RevokedJWTSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = RevokedJWTS.AfterDeleteHooks.RunHooks(ctx, exec, RevokedJWTSlice{o})

	}

	return err
}

// primaryKeyVals returns the primary key values of the RevokedJWT
func (o *RevokedJWT) primaryKeyVals() bob.Expression {
	return sqlite.Arg(o.ID)
}

func (o *RevokedJWT) pkEQ() dialect.Expression {
	return sqlite.Quote("revoked_jwt", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		return o.primaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the RevokedJWT
func (o *RevokedJWT) Update(ctx context.Context, exec bob.Executor, s *RevokedJWTSetter) error {
	v, err := RevokedJWTS.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	*o = *v

	return nil
}

// Delete deletes a single RevokedJWT record with an executor
func (o *RevokedJWT) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := RevokedJWTS.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the RevokedJWT using the executor
func (o *RevokedJWT) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := RevokedJWTS.Query(
		sm.Where(RevokedJWTS.Columns.ID.EQ(sqlite.Arg(o.ID))),
	).One(ctx, exec)
	if err != nil {
		return err
	}

	*o = *o2

	return nil
}

// AfterQueryHook is called after RevokedJWTSlice is retrieved from the database
func (o RevokedJWTSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = RevokedJWTS.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = RevokedJWTS.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = RevokedJWTS.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = RevokedJWTS.AfterDeleteHooks.RunHooks(ctx, exec, o)

	}

	return err
}

func (o RevokedJWTSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return sqlite.Raw("NULL")
	}

	return sqlite.Quote("revoked_jwt", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.StringWriter, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.primaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o RevokedJWTSlice) copyMatchingRows(from ...*RevokedJWT) {
	for i, old := range o {
		for _, new := range from {
			if new.ID != old.ID {
				continue
			}

			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o RevokedJWTSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return RevokedJWTS.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *RevokedJWT:
				o.copyMatchingRows(retrieved)
			case []*RevokedJWT:
				o.copyMatchingRows(retrieved...)
			case RevokedJWTSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a RevokedJWT or a slice of RevokedJWT
				// then run the AfterUpdateHooks on the slice
				_, err = RevokedJWTS.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o RevokedJWTSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return RevokedJWTS.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *RevokedJWT:
				o.copyMatchingRows(retrieved)
			case []*RevokedJWT:
				o.copyMatchingRows(retrieved...)
			case RevokedJWTSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a RevokedJWT or a slice of RevokedJWT
				// then run the AfterDeleteHooks on the slice
				_, err = RevokedJWTS.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

func (o RevokedJWTSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals RevokedJWTSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := RevokedJWTS.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o RevokedJWTSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := RevokedJWTS.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o RevokedJWTSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := RevokedJWTS.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

type revokedJWTWhere[Q sqlite.Filterable] struct {
	ID                    sqlite.WhereMod[Q, string]
	JWTID                 sqlite.WhereNullMod[Q, string]
	Username              sqlite.WhereNullMod[Q, string]
	DeleteAllTokensBefore sqlite.WhereNullMod[Q, time.Time]
	CreatedAt             sqlite.WhereMod[Q, time.Time]
}

func (revokedJWTWhere[Q]) AliasedAs(alias string) revokedJWTWhere[Q] {
	return buildRevokedJWTWhere[Q](buildRevokedJWTColumns(alias))
}

func buildRevokedJWTWhere[Q sqlite.Filterable](cols revokedJWTColumns) revokedJWTWhere[Q] {
	return revokedJWTWhere[Q]{
		ID:                    sqlite.Where[Q, string](cols.ID.Expression),
		JWTID:                 sqlite.WhereNull[Q, string](cols.JWTID.Expression),
		Username:              sqlite.WhereNull[Q, string](cols.Username.Expression),
		DeleteAllTokensBefore: sqlite.WhereNull[Q, time.Time](cols.DeleteAllTokensBefore.Expression),
		CreatedAt:             sqlite.Where[Q, time.Time](cols.CreatedAt.Expression),
	}
}
