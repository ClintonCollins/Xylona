// Code generated by BobGen sqlite v0.30.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"errors"
	"fmt"
	"io"

	"github.com/aarondl/opt/null"
	"github.com/aarondl/opt/omit"
	"github.com/aarondl/opt/omitnull"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/sqlite"
	"github.com/stephenafamo/bob/dialect/sqlite/dialect"
	"github.com/stephenafamo/bob/dialect/sqlite/dm"
	"github.com/stephenafamo/bob/dialect/sqlite/sm"
	"github.com/stephenafamo/bob/dialect/sqlite/um"
	"github.com/stephenafamo/bob/expr"
	"github.com/stephenafamo/bob/mods"
)

// Node is an object representing the database table.
type Node struct {
	ID        string           `db:"id,pk" `
	Name      string           `db:"name" `
	SecretKey null.Val[string] `db:"secret_key" `
	IsLocal   bool             `db:"is_local" `
	Host      string           `db:"host" `
	Port      int32            `db:"port" `

	R nodeR `db:"-" `
}

// NodeSlice is an alias for a slice of pointers to Node.
// This should almost always be used instead of []*Node.
type NodeSlice []*Node

// Nodes contains methods to work with the node table
var Nodes = sqlite.NewTablex[*Node, NodeSlice, *NodeSetter]("", "node")

// NodesQuery is a query on the node table
type NodesQuery = *sqlite.ViewQuery[*Node, NodeSlice]

// nodeR is where relationships are stored.
type nodeR struct {
	GameServers    GameServerSlice    // fk_game_server_0
	NodeSyncQueues NodeSyncQueueSlice // fk_node_sync_queue_0
}

type nodeColumnNames struct {
	ID        string
	Name      string
	SecretKey string
	IsLocal   string
	Host      string
	Port      string
}

var NodeColumns = buildNodeColumns("node")

type nodeColumns struct {
	tableAlias string
	ID         sqlite.Expression
	Name       sqlite.Expression
	SecretKey  sqlite.Expression
	IsLocal    sqlite.Expression
	Host       sqlite.Expression
	Port       sqlite.Expression
}

func (c nodeColumns) Alias() string {
	return c.tableAlias
}

func (nodeColumns) AliasedAs(alias string) nodeColumns {
	return buildNodeColumns(alias)
}

func buildNodeColumns(alias string) nodeColumns {
	return nodeColumns{
		tableAlias: alias,
		ID:         sqlite.Quote(alias, "id"),
		Name:       sqlite.Quote(alias, "name"),
		SecretKey:  sqlite.Quote(alias, "secret_key"),
		IsLocal:    sqlite.Quote(alias, "is_local"),
		Host:       sqlite.Quote(alias, "host"),
		Port:       sqlite.Quote(alias, "port"),
	}
}

type nodeWhere[Q sqlite.Filterable] struct {
	ID        sqlite.WhereMod[Q, string]
	Name      sqlite.WhereMod[Q, string]
	SecretKey sqlite.WhereNullMod[Q, string]
	IsLocal   sqlite.WhereMod[Q, bool]
	Host      sqlite.WhereMod[Q, string]
	Port      sqlite.WhereMod[Q, int32]
}

func (nodeWhere[Q]) AliasedAs(alias string) nodeWhere[Q] {
	return buildNodeWhere[Q](buildNodeColumns(alias))
}

func buildNodeWhere[Q sqlite.Filterable](cols nodeColumns) nodeWhere[Q] {
	return nodeWhere[Q]{
		ID:        sqlite.Where[Q, string](cols.ID),
		Name:      sqlite.Where[Q, string](cols.Name),
		SecretKey: sqlite.WhereNull[Q, string](cols.SecretKey),
		IsLocal:   sqlite.Where[Q, bool](cols.IsLocal),
		Host:      sqlite.Where[Q, string](cols.Host),
		Port:      sqlite.Where[Q, int32](cols.Port),
	}
}

// NodeSetter is used for insert/upsert/update operations
// All values are optional, and do not have to be set
// Generated columns are not included
type NodeSetter struct {
	ID        omit.Val[string]     `db:"id,pk" `
	Name      omit.Val[string]     `db:"name" `
	SecretKey omitnull.Val[string] `db:"secret_key" `
	IsLocal   omit.Val[bool]       `db:"is_local" `
	Host      omit.Val[string]     `db:"host" `
	Port      omit.Val[int32]      `db:"port" `
}

func (s NodeSetter) SetColumns() []string {
	vals := make([]string, 0, 6)
	if !s.ID.IsUnset() {
		vals = append(vals, "id")
	}

	if !s.Name.IsUnset() {
		vals = append(vals, "name")
	}

	if !s.SecretKey.IsUnset() {
		vals = append(vals, "secret_key")
	}

	if !s.IsLocal.IsUnset() {
		vals = append(vals, "is_local")
	}

	if !s.Host.IsUnset() {
		vals = append(vals, "host")
	}

	if !s.Port.IsUnset() {
		vals = append(vals, "port")
	}

	return vals
}

func (s NodeSetter) Overwrite(t *Node) {
	if !s.ID.IsUnset() {
		t.ID, _ = s.ID.Get()
	}
	if !s.Name.IsUnset() {
		t.Name, _ = s.Name.Get()
	}
	if !s.SecretKey.IsUnset() {
		t.SecretKey, _ = s.SecretKey.GetNull()
	}
	if !s.IsLocal.IsUnset() {
		t.IsLocal, _ = s.IsLocal.Get()
	}
	if !s.Host.IsUnset() {
		t.Host, _ = s.Host.Get()
	}
	if !s.Port.IsUnset() {
		t.Port, _ = s.Port.Get()
	}
}

func (s *NodeSetter) Apply(q *dialect.InsertQuery) {
	q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
		return Nodes.BeforeInsertHooks.RunHooks(ctx, exec, s)
	})

	if len(q.Table.Columns) == 0 {
		q.Table.Columns = s.SetColumns()
	}

	q.AppendValues(bob.ExpressionFunc(func(ctx context.Context, w io.Writer, d bob.Dialect, start int) ([]any, error) {
		vals := make([]bob.Expression, 0, 6)
		if !s.ID.IsUnset() {
			vals = append(vals, sqlite.Arg(s.ID))
		}

		if !s.Name.IsUnset() {
			vals = append(vals, sqlite.Arg(s.Name))
		}

		if !s.SecretKey.IsUnset() {
			vals = append(vals, sqlite.Arg(s.SecretKey))
		}

		if !s.IsLocal.IsUnset() {
			vals = append(vals, sqlite.Arg(s.IsLocal))
		}

		if !s.Host.IsUnset() {
			vals = append(vals, sqlite.Arg(s.Host))
		}

		if !s.Port.IsUnset() {
			vals = append(vals, sqlite.Arg(s.Port))
		}

		return bob.ExpressSlice(ctx, w, d, start, vals, "", ", ", "")
	}))
}

func (s NodeSetter) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return um.Set(s.Expressions()...)
}

func (s NodeSetter) Expressions(prefix ...string) []bob.Expression {
	exprs := make([]bob.Expression, 0, 6)

	if !s.ID.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "id")...),
			sqlite.Arg(s.ID),
		}})
	}

	if !s.Name.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "name")...),
			sqlite.Arg(s.Name),
		}})
	}

	if !s.SecretKey.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "secret_key")...),
			sqlite.Arg(s.SecretKey),
		}})
	}

	if !s.IsLocal.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "is_local")...),
			sqlite.Arg(s.IsLocal),
		}})
	}

	if !s.Host.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "host")...),
			sqlite.Arg(s.Host),
		}})
	}

	if !s.Port.IsUnset() {
		exprs = append(exprs, expr.Join{Sep: " = ", Exprs: []bob.Expression{
			sqlite.Quote(append(prefix, "port")...),
			sqlite.Arg(s.Port),
		}})
	}

	return exprs
}

// FindNode retrieves a single record by primary key
// If cols is empty Find will return all columns.
func FindNode(ctx context.Context, exec bob.Executor, IDPK string, cols ...string) (*Node, error) {
	if len(cols) == 0 {
		return Nodes.Query(
			SelectWhere.Nodes.ID.EQ(IDPK),
		).One(ctx, exec)
	}

	return Nodes.Query(
		SelectWhere.Nodes.ID.EQ(IDPK),
		sm.Columns(Nodes.Columns().Only(cols...)),
	).One(ctx, exec)
}

// NodeExists checks the presence of a single record by primary key
func NodeExists(ctx context.Context, exec bob.Executor, IDPK string) (bool, error) {
	return Nodes.Query(
		SelectWhere.Nodes.ID.EQ(IDPK),
	).Exists(ctx, exec)
}

// AfterQueryHook is called after Node is retrieved from the database
func (o *Node) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = Nodes.AfterSelectHooks.RunHooks(ctx, exec, NodeSlice{o})
	case bob.QueryTypeInsert:
		ctx, err = Nodes.AfterInsertHooks.RunHooks(ctx, exec, NodeSlice{o})
	case bob.QueryTypeUpdate:
		ctx, err = Nodes.AfterUpdateHooks.RunHooks(ctx, exec, NodeSlice{o})
	case bob.QueryTypeDelete:
		ctx, err = Nodes.AfterDeleteHooks.RunHooks(ctx, exec, NodeSlice{o})
	}

	return err
}

// PrimaryKeyVals returns the primary key values of the Node
func (o *Node) PrimaryKeyVals() bob.Expression {
	return sqlite.Arg(o.ID)
}

func (o *Node) pkEQ() dialect.Expression {
	return sqlite.Quote("node", "id").EQ(bob.ExpressionFunc(func(ctx context.Context, w io.Writer, d bob.Dialect, start int) ([]any, error) {
		return o.PrimaryKeyVals().WriteSQL(ctx, w, d, start)
	}))
}

// Update uses an executor to update the Node
func (o *Node) Update(ctx context.Context, exec bob.Executor, s *NodeSetter) error {
	v, err := Nodes.Update(s.UpdateMod(), um.Where(o.pkEQ())).One(ctx, exec)
	if err != nil {
		return err
	}

	o.R = v.R
	*o = *v

	return nil
}

// Delete deletes a single Node record with an executor
func (o *Node) Delete(ctx context.Context, exec bob.Executor) error {
	_, err := Nodes.Delete(dm.Where(o.pkEQ())).Exec(ctx, exec)
	return err
}

// Reload refreshes the Node using the executor
func (o *Node) Reload(ctx context.Context, exec bob.Executor) error {
	o2, err := Nodes.Query(
		SelectWhere.Nodes.ID.EQ(o.ID),
	).One(ctx, exec)
	if err != nil {
		return err
	}
	o2.R = o.R
	*o = *o2

	return nil
}

// AfterQueryHook is called after NodeSlice is retrieved from the database
func (o NodeSlice) AfterQueryHook(ctx context.Context, exec bob.Executor, queryType bob.QueryType) error {
	var err error

	switch queryType {
	case bob.QueryTypeSelect:
		ctx, err = Nodes.AfterSelectHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeInsert:
		ctx, err = Nodes.AfterInsertHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeUpdate:
		ctx, err = Nodes.AfterUpdateHooks.RunHooks(ctx, exec, o)
	case bob.QueryTypeDelete:
		ctx, err = Nodes.AfterDeleteHooks.RunHooks(ctx, exec, o)
	}

	return err
}

func (o NodeSlice) pkIN() dialect.Expression {
	if len(o) == 0 {
		return sqlite.Raw("NULL")
	}

	return sqlite.Quote("node", "id").In(bob.ExpressionFunc(func(ctx context.Context, w io.Writer, d bob.Dialect, start int) ([]any, error) {
		pkPairs := make([]bob.Expression, len(o))
		for i, row := range o {
			pkPairs[i] = row.PrimaryKeyVals()
		}
		return bob.ExpressSlice(ctx, w, d, start, pkPairs, "", ", ", "")
	}))
}

// copyMatchingRows finds models in the given slice that have the same primary key
// then it first copies the existing relationships from the old model to the new model
// and then replaces the old model in the slice with the new model
func (o NodeSlice) copyMatchingRows(from ...*Node) {
	for i, old := range o {
		for _, new := range from {
			if new.ID != old.ID {
				continue
			}
			new.R = old.R
			o[i] = new
			break
		}
	}
}

// UpdateMod modifies an update query with "WHERE primary_key IN (o...)"
func (o NodeSlice) UpdateMod() bob.Mod[*dialect.UpdateQuery] {
	return bob.ModFunc[*dialect.UpdateQuery](func(q *dialect.UpdateQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Nodes.BeforeUpdateHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Node:
				o.copyMatchingRows(retrieved)
			case []*Node:
				o.copyMatchingRows(retrieved...)
			case NodeSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Node or a slice of Node
				// then run the AfterUpdateHooks on the slice
				_, err = Nodes.AfterUpdateHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

// DeleteMod modifies an delete query with "WHERE primary_key IN (o...)"
func (o NodeSlice) DeleteMod() bob.Mod[*dialect.DeleteQuery] {
	return bob.ModFunc[*dialect.DeleteQuery](func(q *dialect.DeleteQuery) {
		q.AppendHooks(func(ctx context.Context, exec bob.Executor) (context.Context, error) {
			return Nodes.BeforeDeleteHooks.RunHooks(ctx, exec, o)
		})

		q.AppendLoader(bob.LoaderFunc(func(ctx context.Context, exec bob.Executor, retrieved any) error {
			var err error
			switch retrieved := retrieved.(type) {
			case *Node:
				o.copyMatchingRows(retrieved)
			case []*Node:
				o.copyMatchingRows(retrieved...)
			case NodeSlice:
				o.copyMatchingRows(retrieved...)
			default:
				// If the retrieved value is not a Node or a slice of Node
				// then run the AfterDeleteHooks on the slice
				_, err = Nodes.AfterDeleteHooks.RunHooks(ctx, exec, o)
			}

			return err
		}))

		q.AppendWhere(o.pkIN())
	})
}

func (o NodeSlice) UpdateAll(ctx context.Context, exec bob.Executor, vals NodeSetter) error {
	if len(o) == 0 {
		return nil
	}

	_, err := Nodes.Update(vals.UpdateMod(), o.UpdateMod()).All(ctx, exec)
	return err
}

func (o NodeSlice) DeleteAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	_, err := Nodes.Delete(o.DeleteMod()).Exec(ctx, exec)
	return err
}

func (o NodeSlice) ReloadAll(ctx context.Context, exec bob.Executor) error {
	if len(o) == 0 {
		return nil
	}

	o2, err := Nodes.Query(sm.Where(o.pkIN())).All(ctx, exec)
	if err != nil {
		return err
	}

	o.copyMatchingRows(o2...)

	return nil
}

type nodeJoins[Q dialect.Joinable] struct {
	typ            string
	GameServers    func(context.Context) modAs[Q, gameServerColumns]
	NodeSyncQueues func(context.Context) modAs[Q, nodeSyncQueueColumns]
}

func (j nodeJoins[Q]) aliasedAs(alias string) nodeJoins[Q] {
	return buildNodeJoins[Q](buildNodeColumns(alias), j.typ)
}

func buildNodeJoins[Q dialect.Joinable](cols nodeColumns, typ string) nodeJoins[Q] {
	return nodeJoins[Q]{
		typ:            typ,
		GameServers:    nodesJoinGameServers[Q](cols, typ),
		NodeSyncQueues: nodesJoinNodeSyncQueues[Q](cols, typ),
	}
}

func nodesJoinGameServers[Q dialect.Joinable](from nodeColumns, typ string) func(context.Context) modAs[Q, gameServerColumns] {
	return func(ctx context.Context) modAs[Q, gameServerColumns] {
		return modAs[Q, gameServerColumns]{
			c: GameServerColumns,
			f: func(to gameServerColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, GameServers.Name().As(to.Alias())).On(
						to.NodeID.EQ(from.ID),
					))
				}

				return mods
			},
		}
	}
}

func nodesJoinNodeSyncQueues[Q dialect.Joinable](from nodeColumns, typ string) func(context.Context) modAs[Q, nodeSyncQueueColumns] {
	return func(ctx context.Context) modAs[Q, nodeSyncQueueColumns] {
		return modAs[Q, nodeSyncQueueColumns]{
			c: NodeSyncQueueColumns,
			f: func(to nodeSyncQueueColumns) bob.Mod[Q] {
				mods := make(mods.QueryMods[Q], 0, 1)

				{
					mods = append(mods, dialect.Join[Q](typ, NodeSyncQueues.Name().As(to.Alias())).On(
						to.NodeID.EQ(from.ID),
					))
				}

				return mods
			},
		}
	}
}

// GameServers starts a query for related objects on game_server
func (o *Node) GameServers(mods ...bob.Mod[*dialect.SelectQuery]) GameServersQuery {
	return GameServers.Query(append(mods,
		sm.Where(GameServerColumns.NodeID.EQ(sqlite.Arg(o.ID))),
	)...)
}

func (os NodeSlice) GameServers(mods ...bob.Mod[*dialect.SelectQuery]) GameServersQuery {
	PKArgs := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgs[i] = sqlite.ArgGroup(o.ID)
	}

	return GameServers.Query(append(mods,
		sm.Where(sqlite.Group(GameServerColumns.NodeID).In(PKArgs...)),
	)...)
}

// NodeSyncQueues starts a query for related objects on node_sync_queue
func (o *Node) NodeSyncQueues(mods ...bob.Mod[*dialect.SelectQuery]) NodeSyncQueuesQuery {
	return NodeSyncQueues.Query(append(mods,
		sm.Where(NodeSyncQueueColumns.NodeID.EQ(sqlite.Arg(o.ID))),
	)...)
}

func (os NodeSlice) NodeSyncQueues(mods ...bob.Mod[*dialect.SelectQuery]) NodeSyncQueuesQuery {
	PKArgs := make([]bob.Expression, len(os))
	for i, o := range os {
		PKArgs[i] = sqlite.ArgGroup(o.ID)
	}

	return NodeSyncQueues.Query(append(mods,
		sm.Where(sqlite.Group(NodeSyncQueueColumns.NodeID).In(PKArgs...)),
	)...)
}

func (o *Node) Preload(name string, retrieved any) error {
	if o == nil {
		return nil
	}

	switch name {
	case "GameServers":
		rels, ok := retrieved.(GameServerSlice)
		if !ok {
			return fmt.Errorf("node cannot load %T as %q", retrieved, name)
		}

		o.R.GameServers = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.Node = o
			}
		}
		return nil
	case "NodeSyncQueues":
		rels, ok := retrieved.(NodeSyncQueueSlice)
		if !ok {
			return fmt.Errorf("node cannot load %T as %q", retrieved, name)
		}

		o.R.NodeSyncQueues = rels

		for _, rel := range rels {
			if rel != nil {
				rel.R.Node = o
			}
		}
		return nil
	default:
		return fmt.Errorf("node has no relationship %q", name)
	}
}

func ThenLoadNodeGameServers(queryMods ...bob.Mod[*dialect.SelectQuery]) sqlite.Loader {
	return sqlite.Loader(func(ctx context.Context, exec bob.Executor, retrieved any) error {
		loader, isLoader := retrieved.(interface {
			LoadNodeGameServers(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
		})
		if !isLoader {
			return fmt.Errorf("object %T cannot load NodeGameServers", retrieved)
		}

		err := loader.LoadNodeGameServers(ctx, exec, queryMods...)

		// Don't cause an issue due to missing relationships
		if errors.Is(err, sql.ErrNoRows) {
			return nil
		}

		return err
	})
}

// LoadNodeGameServers loads the node's GameServers into the .R struct
func (o *Node) LoadNodeGameServers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.GameServers = nil

	related, err := o.GameServers(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.Node = o
	}

	o.R.GameServers = related
	return nil
}

// LoadNodeGameServers loads the node's GameServers into the .R struct
func (os NodeSlice) LoadNodeGameServers(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	gameServers, err := os.GameServers(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		o.R.GameServers = nil
	}

	for _, o := range os {
		for _, rel := range gameServers {
			if o.ID != rel.NodeID {
				continue
			}

			rel.R.Node = o

			o.R.GameServers = append(o.R.GameServers, rel)
		}
	}

	return nil
}

func ThenLoadNodeNodeSyncQueues(queryMods ...bob.Mod[*dialect.SelectQuery]) sqlite.Loader {
	return sqlite.Loader(func(ctx context.Context, exec bob.Executor, retrieved any) error {
		loader, isLoader := retrieved.(interface {
			LoadNodeNodeSyncQueues(context.Context, bob.Executor, ...bob.Mod[*dialect.SelectQuery]) error
		})
		if !isLoader {
			return fmt.Errorf("object %T cannot load NodeNodeSyncQueues", retrieved)
		}

		err := loader.LoadNodeNodeSyncQueues(ctx, exec, queryMods...)

		// Don't cause an issue due to missing relationships
		if errors.Is(err, sql.ErrNoRows) {
			return nil
		}

		return err
	})
}

// LoadNodeNodeSyncQueues loads the node's NodeSyncQueues into the .R struct
func (o *Node) LoadNodeNodeSyncQueues(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if o == nil {
		return nil
	}

	// Reset the relationship
	o.R.NodeSyncQueues = nil

	related, err := o.NodeSyncQueues(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, rel := range related {
		rel.R.Node = o
	}

	o.R.NodeSyncQueues = related
	return nil
}

// LoadNodeNodeSyncQueues loads the node's NodeSyncQueues into the .R struct
func (os NodeSlice) LoadNodeNodeSyncQueues(ctx context.Context, exec bob.Executor, mods ...bob.Mod[*dialect.SelectQuery]) error {
	if len(os) == 0 {
		return nil
	}

	nodeSyncQueues, err := os.NodeSyncQueues(mods...).All(ctx, exec)
	if err != nil {
		return err
	}

	for _, o := range os {
		o.R.NodeSyncQueues = nil
	}

	for _, o := range os {
		for _, rel := range nodeSyncQueues {
			if o.ID != rel.NodeID {
				continue
			}

			rel.R.Node = o

			o.R.NodeSyncQueues = append(o.R.NodeSyncQueues, rel)
		}
	}

	return nil
}

func insertNodeGameServers0(ctx context.Context, exec bob.Executor, gameServers1 []*GameServerSetter, node0 *Node) (GameServerSlice, error) {
	for i := range gameServers1 {
		gameServers1[i].NodeID = omit.From(node0.ID)
	}

	ret, err := GameServers.Insert(bob.ToMods(gameServers1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertNodeGameServers0: %w", err)
	}

	return ret, nil
}

func attachNodeGameServers0(ctx context.Context, exec bob.Executor, count int, gameServers1 GameServerSlice, node0 *Node) (GameServerSlice, error) {
	setter := &GameServerSetter{
		NodeID: omit.From(node0.ID),
	}

	err := gameServers1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachNodeGameServers0: %w", err)
	}

	return gameServers1, nil
}

func (node0 *Node) InsertGameServers(ctx context.Context, exec bob.Executor, related ...*GameServerSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	gameServers1, err := insertNodeGameServers0(ctx, exec, related, node0)
	if err != nil {
		return err
	}

	node0.R.GameServers = append(node0.R.GameServers, gameServers1...)

	for _, rel := range gameServers1 {
		rel.R.Node = node0
	}
	return nil
}

func (node0 *Node) AttachGameServers(ctx context.Context, exec bob.Executor, related ...*GameServer) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	gameServers1 := GameServerSlice(related)

	_, err = attachNodeGameServers0(ctx, exec, len(related), gameServers1, node0)
	if err != nil {
		return err
	}

	node0.R.GameServers = append(node0.R.GameServers, gameServers1...)

	for _, rel := range related {
		rel.R.Node = node0
	}

	return nil
}

func insertNodeNodeSyncQueues0(ctx context.Context, exec bob.Executor, nodeSyncQueues1 []*NodeSyncQueueSetter, node0 *Node) (NodeSyncQueueSlice, error) {
	for i := range nodeSyncQueues1 {
		nodeSyncQueues1[i].NodeID = omit.From(node0.ID)
	}

	ret, err := NodeSyncQueues.Insert(bob.ToMods(nodeSyncQueues1...)).All(ctx, exec)
	if err != nil {
		return ret, fmt.Errorf("insertNodeNodeSyncQueues0: %w", err)
	}

	return ret, nil
}

func attachNodeNodeSyncQueues0(ctx context.Context, exec bob.Executor, count int, nodeSyncQueues1 NodeSyncQueueSlice, node0 *Node) (NodeSyncQueueSlice, error) {
	setter := &NodeSyncQueueSetter{
		NodeID: omit.From(node0.ID),
	}

	err := nodeSyncQueues1.UpdateAll(ctx, exec, *setter)
	if err != nil {
		return nil, fmt.Errorf("attachNodeNodeSyncQueues0: %w", err)
	}

	return nodeSyncQueues1, nil
}

func (node0 *Node) InsertNodeSyncQueues(ctx context.Context, exec bob.Executor, related ...*NodeSyncQueueSetter) error {
	if len(related) == 0 {
		return nil
	}

	var err error

	nodeSyncQueues1, err := insertNodeNodeSyncQueues0(ctx, exec, related, node0)
	if err != nil {
		return err
	}

	node0.R.NodeSyncQueues = append(node0.R.NodeSyncQueues, nodeSyncQueues1...)

	for _, rel := range nodeSyncQueues1 {
		rel.R.Node = node0
	}
	return nil
}

func (node0 *Node) AttachNodeSyncQueues(ctx context.Context, exec bob.Executor, related ...*NodeSyncQueue) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	nodeSyncQueues1 := NodeSyncQueueSlice(related)

	_, err = attachNodeNodeSyncQueues0(ctx, exec, len(related), nodeSyncQueues1, node0)
	if err != nil {
		return err
	}

	node0.R.NodeSyncQueues = append(node0.R.NodeSyncQueues, nodeSyncQueues1...)

	for _, rel := range related {
		rel.R.Node = node0
	}

	return nil
}
