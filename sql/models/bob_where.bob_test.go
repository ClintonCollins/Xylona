// Code generated by BobGen sqlite v0.50.0. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"strings"
	"testing"

	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/sqlite"
	"github.com/stephenafamo/bob/dialect/sqlite/sm"
)

// TestAlertHistoryHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestAlertHistoryHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertHistories.NameExpr()),
			SelectWhere.AlertHistories.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("AlertRule", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertHistories.NameExpr()),
			SelectWhere.AlertHistories.R.HasAlertRule(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertRule: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertRule: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestAlertRuleHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestAlertRuleHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("AlertHistories", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertRules.NameExpr()),
			SelectWhere.AlertRules.R.HasAlertHistories(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertHistories: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertHistories: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("NotificationChannel", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertRules.NameExpr()),
			SelectWhere.AlertRules.R.HasNotificationChannel(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNotificationChannel: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNotificationChannel: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Node", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertRules.NameExpr()),
			SelectWhere.AlertRules.R.HasNode(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNode: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNode: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertRules.NameExpr()),
			SelectWhere.AlertRules.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("AlertStates", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertRules.NameExpr()),
			SelectWhere.AlertRules.R.HasAlertStates(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertStates: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertStates: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestAlertStateHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestAlertStateHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("AlertRule", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(AlertStates.NameExpr()),
			SelectWhere.AlertStates.R.HasAlertRule(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertRule: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertRule: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestDNSRecordBindingHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestDNSRecordBindingHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(DNSRecordBindings.NameExpr()),
			SelectWhere.DNSRecordBindings.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServers", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Games.NameExpr()),
			SelectWhere.Games.R.HasGameServers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServers: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("DNSRecordBinding", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasDNSRecordBinding(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasDNSRecordBinding: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasDNSRecordBinding: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("IP", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasIP(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasIP: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasIP: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Node", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasNode(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNode: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNode: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Game", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGame(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGame: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGame: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerBackups", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerBackups(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerBackups: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerBackups: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerDiagnosis", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerDiagnosis(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerDiagnosis: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerDiagnosis: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerLifecycleEvents", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerLifecycleEvents(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerLifecycleEvents: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerLifecycleEvents: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerMapShare", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerMapShare(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerMapShare: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerMapShare: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerMetricsHistories", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerMetricsHistories(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerMetricsHistories: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerMetricsHistories: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerMinecraftMap", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerMinecraftMap(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerMinecraftMap: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerMinecraftMap: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerOperationEvents", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerOperationEvents(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerOperationEvents: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerOperationEvents: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerPalworldMap", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerPalworldMap(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerPalworldMap: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerPalworldMap: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerSecrets", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerSecrets(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerSecrets: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerSecrets: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerSevenDaysToDieMap", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerSevenDaysToDieMap(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerSevenDaysToDieMap: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerSevenDaysToDieMap: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerSevenDaysToDieMapShares", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasGameServerSevenDaysToDieMapShares(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerSevenDaysToDieMapShares: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerSevenDaysToDieMapShares: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("InstalledMods", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasInstalledMods(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasInstalledMods: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasInstalledMods: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ScheduledTasks", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasScheduledTasks(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasScheduledTasks: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasScheduledTasks: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UserRoleAssignments", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServers.NameExpr()),
			SelectWhere.GameServers.R.HasUserRoleAssignments(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUserRoleAssignments: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUserRoleAssignments: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerBackupHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerBackupHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Node", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerBackups.NameExpr()),
			SelectWhere.GameServerBackups.R.HasNode(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNode: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNode: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerBackups.NameExpr()),
			SelectWhere.GameServerBackups.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerDiagnosisHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerDiagnosisHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerDiagnoses.NameExpr()),
			SelectWhere.GameServerDiagnoses.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerLifecycleEventHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerLifecycleEventHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerLifecycleEvents.NameExpr()),
			SelectWhere.GameServerLifecycleEvents.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerMapShareHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerMapShareHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerMapShares.NameExpr()),
			SelectWhere.GameServerMapShares.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerMetricsHistoryHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerMetricsHistoryHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerMetricsHistories.NameExpr()),
			SelectWhere.GameServerMetricsHistories.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerMinecraftMapHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerMinecraftMapHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("UpdatedByUserUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerMinecraftMaps.NameExpr()),
			SelectWhere.GameServerMinecraftMaps.R.HasUpdatedByUserUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerMinecraftMaps.NameExpr()),
			SelectWhere.GameServerMinecraftMaps.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerOperationEventHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerOperationEventHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerOperationEvents.NameExpr()),
			SelectWhere.GameServerOperationEvents.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerPalworldMapHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerPalworldMapHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("UpdatedByUserUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerPalworldMaps.NameExpr()),
			SelectWhere.GameServerPalworldMaps.R.HasUpdatedByUserUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerPalworldMaps.NameExpr()),
			SelectWhere.GameServerPalworldMaps.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerSecretHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerSecretHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("UpdatedByUserUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerSecrets.NameExpr()),
			SelectWhere.GameServerSecrets.R.HasUpdatedByUserUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerSecrets.NameExpr()),
			SelectWhere.GameServerSecrets.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerSevenDaysToDieMapHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerSevenDaysToDieMapHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("UpdatedByUserUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerSevenDaysToDieMaps.NameExpr()),
			SelectWhere.GameServerSevenDaysToDieMaps.R.HasUpdatedByUserUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerSevenDaysToDieMaps.NameExpr()),
			SelectWhere.GameServerSevenDaysToDieMaps.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerSevenDaysToDieMapShareHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerSevenDaysToDieMapShareHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CreatedByUserUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerSevenDaysToDieMapShares.NameExpr()),
			SelectWhere.GameServerSevenDaysToDieMapShares.R.HasCreatedByUserUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCreatedByUserUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCreatedByUserUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerSevenDaysToDieMapShares.NameExpr()),
			SelectWhere.GameServerSevenDaysToDieMapShares.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerStatusPageHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerStatusPageHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("PublicIdentifierGameServerStatusPageIdentifier", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerStatusPages.NameExpr()),
			SelectWhere.GameServerStatusPages.R.HasPublicIdentifierGameServerStatusPageIdentifier(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPublicIdentifierGameServerStatusPageIdentifier: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPublicIdentifierGameServerStatusPageIdentifier: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerStatusPages.NameExpr()),
			SelectWhere.GameServerStatusPages.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestGameServerStatusPageIdentifierHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestGameServerStatusPageIdentifierHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("PublicIdentifierGameServerStatusPage", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(GameServerStatusPageIdentifiers.NameExpr()),
			SelectWhere.GameServerStatusPageIdentifiers.R.HasPublicIdentifierGameServerStatusPage(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPublicIdentifierGameServerStatusPage: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPublicIdentifierGameServerStatusPage: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestInstalledModHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestInstalledModHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(InstalledMods.NameExpr()),
			SelectWhere.InstalledMods.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("InstalledModFiles", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(InstalledMods.NameExpr()),
			SelectWhere.InstalledMods.R.HasInstalledModFiles(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasInstalledModFiles: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasInstalledModFiles: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestInstalledModFileHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestInstalledModFileHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("InstalledMod", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(InstalledModFiles.NameExpr()),
			SelectWhere.InstalledModFiles.R.HasInstalledMod(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasInstalledMod: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasInstalledMod: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestIPHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestIPHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GameServers", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Ips.NameExpr()),
			SelectWhere.Ips.R.HasGameServers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Node", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Ips.NameExpr()),
			SelectWhere.Ips.R.HasNode(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNode: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNode: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestNodeHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestNodeHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("AlertRules", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Nodes.NameExpr()),
			SelectWhere.Nodes.R.HasAlertRules(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertRules: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertRules: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServers", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Nodes.NameExpr()),
			SelectWhere.Nodes.R.HasGameServers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerBackups", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Nodes.NameExpr()),
			SelectWhere.Nodes.R.HasGameServerBackups(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerBackups: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerBackups: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Ips", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Nodes.NameExpr()),
			SelectWhere.Nodes.R.HasIps(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasIps: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasIps: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("NodeMetricsHistories", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Nodes.NameExpr()),
			SelectWhere.Nodes.R.HasNodeMetricsHistories(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNodeMetricsHistories: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNodeMetricsHistories: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("SystemUpdateJobs", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Nodes.NameExpr()),
			SelectWhere.Nodes.R.HasSystemUpdateJobs(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasSystemUpdateJobs: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasSystemUpdateJobs: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestNodeMetricsHistoryHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestNodeMetricsHistoryHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Node", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(NodeMetricsHistories.NameExpr()),
			SelectWhere.NodeMetricsHistories.R.HasNode(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNode: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNode: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestNotificationChannelHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestNotificationChannelHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("AlertRules", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(NotificationChannels.NameExpr()),
			SelectWhere.NotificationChannels.R.HasAlertRules(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertRules: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertRules: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(NotificationChannels.NameExpr()),
			SelectWhere.NotificationChannels.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestPermissionHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestPermissionHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Roles", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Permissions.NameExpr()),
			SelectWhere.Permissions.R.HasRoles(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRoles: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRoles: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRoleHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRoleHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Permissions", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Roles.NameExpr()),
			SelectWhere.Roles.R.HasPermissions(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPermissions: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPermissions: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UserRoleAssignments", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Roles.NameExpr()),
			SelectWhere.Roles.R.HasUserRoleAssignments(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUserRoleAssignments: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUserRoleAssignments: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestRolePermissionHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestRolePermissionHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("Permission", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(RolePermissions.NameExpr()),
			SelectWhere.RolePermissions.R.HasPermission(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasPermission: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasPermission: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Role", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(RolePermissions.NameExpr()),
			SelectWhere.RolePermissions.R.HasRole(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRole: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRole: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestScheduledTaskHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestScheduledTaskHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("CreatedByUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(ScheduledTasks.NameExpr()),
			SelectWhere.ScheduledTasks.R.HasCreatedByUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCreatedByUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCreatedByUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(ScheduledTasks.NameExpr()),
			SelectWhere.ScheduledTasks.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("ScheduledTaskLogs", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(ScheduledTasks.NameExpr()),
			SelectWhere.ScheduledTasks.R.HasScheduledTaskLogs(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasScheduledTaskLogs: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasScheduledTaskLogs: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestScheduledTaskLogHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestScheduledTaskLogHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("ScheduledTask", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(ScheduledTaskLogs.NameExpr()),
			SelectWhere.ScheduledTaskLogs.R.HasScheduledTask(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasScheduledTask: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasScheduledTask: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSystemUpdateJobHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSystemUpdateJobHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("RequestedByUserUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(SystemUpdateJobs.NameExpr()),
			SelectWhere.SystemUpdateJobs.R.HasRequestedByUserUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRequestedByUserUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRequestedByUserUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Node", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(SystemUpdateJobs.NameExpr()),
			SelectWhere.SystemUpdateJobs.R.HasNode(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNode: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNode: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("JobSystemUpdateJobEvents", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(SystemUpdateJobs.NameExpr()),
			SelectWhere.SystemUpdateJobs.R.HasJobSystemUpdateJobEvents(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasJobSystemUpdateJobEvents: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasJobSystemUpdateJobEvents: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestSystemUpdateJobEventHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestSystemUpdateJobEventHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("JobSystemUpdateJob", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(SystemUpdateJobEvents.NameExpr()),
			SelectWhere.SystemUpdateJobEvents.R.HasJobSystemUpdateJob(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasJobSystemUpdateJob: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasJobSystemUpdateJob: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestUserHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestUserHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("AlertHistories", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasAlertHistories(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertHistories: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertHistories: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("AlertRules", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasAlertRules(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasAlertRules: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasAlertRules: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServers", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasGameServers(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServers: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServers: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UpdatedByUserGameServerMinecraftMaps", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUpdatedByUserGameServerMinecraftMaps(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserGameServerMinecraftMaps: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserGameServerMinecraftMaps: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UpdatedByUserGameServerPalworldMaps", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUpdatedByUserGameServerPalworldMaps(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserGameServerPalworldMaps: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserGameServerPalworldMaps: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UpdatedByUserGameServerSecrets", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUpdatedByUserGameServerSecrets(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserGameServerSecrets: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserGameServerSecrets: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UpdatedByUserGameServerSevenDaysToDieMaps", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUpdatedByUserGameServerSevenDaysToDieMaps(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUpdatedByUserGameServerSevenDaysToDieMaps: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUpdatedByUserGameServerSevenDaysToDieMaps: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CreatedByUserGameServerSevenDaysToDieMapShares", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasCreatedByUserGameServerSevenDaysToDieMapShares(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCreatedByUserGameServerSevenDaysToDieMapShares: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCreatedByUserGameServerSevenDaysToDieMapShares: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServerStatusPage", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasGameServerStatusPage(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServerStatusPage: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServerStatusPage: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("NotificationChannels", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasNotificationChannels(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasNotificationChannels: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasNotificationChannels: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("CreatedByScheduledTasks", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasCreatedByScheduledTasks(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasCreatedByScheduledTasks: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasCreatedByScheduledTasks: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("RequestedByUserSystemUpdateJobs", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasRequestedByUserSystemUpdateJobs(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRequestedByUserSystemUpdateJobs: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRequestedByUserSystemUpdateJobs: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GrantedByUserRoleAssignments", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasGrantedByUserRoleAssignments(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGrantedByUserRoleAssignments: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGrantedByUserRoleAssignments: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UserRoleAssignments", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUserRoleAssignments(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUserRoleAssignments: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUserRoleAssignments: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("UserSessions", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(Users.NameExpr()),
			SelectWhere.Users.R.HasUserSessions(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUserSessions: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUserSessions: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestUserRoleAssignmentHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestUserRoleAssignmentHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("GrantedByUser", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(UserRoleAssignments.NameExpr()),
			SelectWhere.UserRoleAssignments.R.HasGrantedByUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGrantedByUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGrantedByUser: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("GameServer", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(UserRoleAssignments.NameExpr()),
			SelectWhere.UserRoleAssignments.R.HasGameServer(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasGameServer: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasGameServer: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("Role", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(UserRoleAssignments.NameExpr()),
			SelectWhere.UserRoleAssignments.R.HasRole(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasRole: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasRole: expected EXISTS in query, got: %s", sql)
		}
	})

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(UserRoleAssignments.NameExpr()),
			SelectWhere.UserRoleAssignments.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}

// TestUserSessionHasRelationsEmitExists verifies that every generated
// Has{Rel} helper produces a correlated EXISTS subquery (semi-join) rather than
// an INNER JOIN, so the parent rows are never multiplied.
func TestUserSessionHasRelationsEmitExists(t *testing.T) {
	ctx := context.Background()

	t.Run("User", func(t *testing.T) {
		q := sqlite.Select(
			sm.From(UserSessions.NameExpr()),
			SelectWhere.UserSessions.R.HasUser(),
		)
		sql, _, err := bob.Build(ctx, q)
		if err != nil {
			t.Fatalf("HasUser: build error: %v", err)
		}
		if !strings.Contains(sql, "EXISTS") {
			t.Errorf("HasUser: expected EXISTS in query, got: %s", sql)
		}
	})
}
